if not RoleBase then
	cc.exports.RoleBase = class("RoleBase", function() return display.newNode() end)
	RoleBase.Animationspeed = 1.0/30.0;
	local Zorder = {}

	RoleBase.Status = {
		--待机
		stand = 1,
		--跑步
		run = 3,

	}

	RoleBase.Status = {
		--
		

	}





end